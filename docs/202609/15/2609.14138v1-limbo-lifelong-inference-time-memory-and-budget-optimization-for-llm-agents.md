---
title: "LIMBO: Lifelong Inference-Time Memory and Budget Optimization for LLM Agents"
title_zh: LIMBO：面向LLM智能体的终身推理时记忆与预算优化
authors: "Siddharth Sharma, Nilesh Prasad Pandey, Onat Gungor, Tajana Rosing"
date: 2026-09-12
pdf: "https://arxiv.org/pdf/2609.14138v1"
tags: ["query:context-mgmt"]
score: 8.0
evidence: 提示预算下经验回放、检索与推理的推理时记忆分配
tldr: "面向终身学习LLM智能体，经验回放会与检索、推理、工具使用和验证争夺有限的提示与计算预算，固定回放策略难以按任务收益分配资源。LIMBO将记忆视为可控制的推理时资源，在线联合优化每个新任务的记忆策略与推理预算，无需改模型、教师监督或离线重训。在LifelongAgentBench三个骨干上，它以最高约83%更低推理成本（平均约53%）取得更优成本-准确率权衡，并接近最强记忆增强基线。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.14138v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 819, \"height\": 538, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.14138v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 783, \"height\": 914, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.14138v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 881, \"height\": 605, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.14138v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1869, \"height\": 964, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.14138v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1556, \"height\": 601, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.14138v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 797, \"height\": 294, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.14138v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1424, \"height\": 355, \"label\": \"Table\"}]"
motivation: 终身LLM智能体依赖经验回放保留旧能力，但回放轨迹会挤占检索、推理、工具调用和验证的提示与计算预算，固定回放策略忽视当前任务是否受益。
method: LIMBO把记忆当作可控的推理时资源，在线单遍学习记忆分配策略，并为每个任务联合优化回放策略与推理预算，不修改底层智能体。
result: "在LifelongAgentBench三个LLM骨干上，LIMBO取得更优成本-准确率权衡，推理成本最高降约83%、平均降约53%时接近最强记忆增强基线。"
conclusion: 结果表明，有效记忆分配可在线学习而非人工设定，LIMBO能跨模型和环境自适应，无需重训，为终身智能体推理时资源优化提供新范式。
---

## 摘要
随着LLM智能体被集成到日益复杂的工作流中，它们必须不断获取新能力，同时保持在先前学习任务上的性能。终身智能体通过经验回放来应对这一问题，即将过去的交互注入提示中，以在推理时利用先前的经验。然而，回放并非没有代价：每条被回放的轨迹都要与检索、推理、工具使用和验证争夺同样有限的提示与计算预算，因此有效的资源分配至关重要。现有方法使用固定的回放策略来分配这些资源，而不考虑回放是否对当前任务有益。我们将此识别为推理时记忆分配，这是终身智能体的一类独特问题，并提出LIMBO：据我们所知，这是首个将记忆视为可控推理时资源，并针对每个到来的任务联合优化记忆策略与推理预算的在线框架。与固定回放策略或需要模型权重、教师监督或离线再训练的先验方法不同，LIMBO在单次遍历中在线学习这种分配，在不修改底层智能体的情况下显式平衡任务性能与推理成本。在LifelongAgentBench上的三个LLM主干模型上，LIMBO实现了优于最先进记忆增强基线的成本-准确率权衡，并在推理成本降低最多约83%（平均约53%）的情况下，几乎匹配所有最强的此类基线。LIMBO无需再训练即可在不同模型和环境间自适应调整其策略，表明有效的分配可以是在线学习得到的，而非人工指定。

## Abstract
As LLM agents become integrated into increasingly complex workflows, they must continually acquire new capabilities while retaining competence on previously learned tasks. Lifelong agents address this through experience replay, injecting past interactions into the prompt to leverage prior experience during inference. However, replay is not free: every replayed trajectory competes with retrieval, reasoning, tool use, and verification for the same limited prompt and compute budget, making effective resource allocation essential. Existing approaches allocate these resources using fixed replay policies, regardless of whether replay is beneficial for the current task. We identify this as inference-time memory allocation, a distinct problem class for lifelong agents, and introduce LIMBO: the first online framework to our knowledge that treats memory as a controllable inference-time resource and jointly optimizes memory strategy and inference budget for each incoming task. Unlike prior approaches that fix the replay policy or require model weights, teacher supervision, or offline retraining, LIMBO learns this allocation online in a single pass, explicitly balancing task performance and inference cost without modifying the underlying agent. Across three LLM backbones on LifelongAgentBench, LIMBO achieves better cost-accuracy tradeoffs than state-of-the-art memory-augmented baselines and nearly matches all strongest such baselines at up to ~83% lower inference cost (~53% on average). LIMBO adapts its policy across models and environments without retraining, demonstrating that effective allocation can be learned online rather than manually specified.

---

## 论文详细总结（自动生成）

# LIMBO 论文中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **背景**：LLM 智能体正被用于持续演化的任务流，需要不断获取新能力，同时保留旧任务上的能力。终身学习智能体通常依赖**经验回放**，把过去成功轨迹注入当前提示，以复用工具调用模式、命令模板和动作格式。
- **核心问题**：回放并非免费。每条被回放的轨迹都会与检索、推理、工具使用、验证等竞争同一份有限的提示长度与推理计算预算。随着任务数增加，回放缓冲区线性增长，注入 token 数呈 \(O(t)\) 增加，推理开销显著上升。
- **现有方法不足**：固定回放策略、固定压缩策略或检索策略，都不判断“当前任务是否值得回放”。固定扩大回放预算会出现收益递减：论文 Figure 1 显示，DBBench 上从 no-replay 到 \(k=16\)，成本增加超过 5 倍，准确率仅提升约 2 点，且 \(k=
