---
title: Cautious Context Steering for Language Model Personalization
title_zh: 语言模型个性化中的谨慎上下文引导
authors: "Gihoon Kim, Jeyoung Lee, Suhan Woo, Sekwon Oh, Minsu Jeon, Hyounsoo Han, Euntai Kim"
date: 2026-08-06
pdf: "https://arxiv.org/pdf/2608.05813v1"
tags: ["query:context-mgmt"]
score: 7.0
evidence: 通过谨慎上下文引导在解码时动态校准上下文影响，优化上下文使用
tldr: 语言模型个性化需要适配不同用户的目标与背景，但专门训练适配器的方法受限于数据稀疏，难以推广到新用户和新领域；而上下文引导虽无需训练，却无法动态调节用户上下文的影响。为此，本文提出谨慎上下文引导CCS，在冻结的骨干模型上附加轻量适配器，对每个生成token判断用户上下文是否应该参与以及参与程度，并通过模仿理想的条件语言模型来学习该策略。仅使用单一数据集训练，CCS就能在域内和四个域外个性化基准上提升生成质量，同时省去了额外的前向传播，显著降低推理成本，展现出对新用户和新领域的鲁棒泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.05813v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1435, \"height\": 926, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.05813v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1417, \"height\": 512, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.05813v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1424, \"height\": 519, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.05813v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1354, \"height\": 1030, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.05813v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 605, \"height\": 320, \"label\": \"Table\"}]"
motivation: 现有per-user训练因数据稀疏难以泛化，而上下文引导固定强度或不控制影响，缺乏逐token的适应性。
method: 在冻结骨干LM上加轻量适配器，逐token学习是否及如何利用用户上下文，并模仿oracle条件LM。
result: 仅单数据集训练，在域内及四个OOD基准上提升生成质量，并省去额外前向传播以降低推理成本。
conclusion: CCS实现了无需用户微调的动态上下文控制，为个性化提供高效且泛化强的方案。
---

## 摘要
将语言模型（LM）个性化以适应用户个体偏好，对于使响应与多样化的目标和背景保持一致至关重要。现有方法通常为每个用户训练一个独立的适配器，或学习一个其分数依赖于用户的奖励模型。尽管这些方法显式地针对每个用户进行优化，但它们必须从有限的观测中学习，因此会遭受数据稀疏性问题，并且对未见用户和领域的泛化能力较差。相比之下，上下文学习（ICL）和上下文引导（CoS）可以直接基于用户上下文对基础LM进行条件化，并利用其预训练能力，而无需针对每个用户进行训练，从而提供更有效的个性化。然而，这两种方法都没有在解码步骤中调整该上下文的影响：ICL使其不受控制，而CoS则应用固定的引导系数，并且每一步需要两次LM前向传播。我们提出了谨慎上下文引导（CCS），它在冻结的主干LM上添加一个轻量级适配器，以在每个token处决定用户上下文是否以及以多强的程度影响生成。该适配器从oracle上下文条件化LM中学习这种行为，并在上下文无帮助时保留基础LM。仅在单个数据集上训练的一个CCS适配器，在域内以及四个分布外个性化基准上均能提高生成质量，展示了对新用户和领域的稳健泛化能力。CCS还避免了针对每个用户的微调以及CoS所需的额外上下文条件化前向传播，从而大幅降低了推理成本。

## Abstract
Personalizing language models (LMs) to individual user preferences is essential for aligning responses with diverse goals and backgrounds. Existing methods typically train a separate adapter for each user or learn a reward model whose scores depend on the user. Despite explicitly optimizing for each user, these methods must learn from limited observations and therefore suffer from data sparsity and poor generalization to unseen users and domains. In-context learning (ICL) and Context Steering (CoS) can instead provide more effective personalization by conditioning the base LM directly on user context and leveraging its pretrained capabilities without per-user training. Yet neither adapts the influence of that context across decoding steps: ICL leaves it uncontrolled, whereas CoS applies a fixed steering coefficient and requires two LM forward passes per step. We propose Cautious Context Steering (CCS), which adds a lightweight adapter to a frozen backbone LM to decide at each token whether and how strongly user context should affect generation. The adapter learns this behavior from an oracle context-conditioned LM and preserves the base LM when the context is not helpful. A single CCS adapter trained on only one dataset improves generation quality both in-domain and across four out-of-distribution personalization benchmarks, demonstrating robust generalization to new users and domains. CCS also avoids per-user fine-tuning and the additional context-conditioned forward pass required by CoS, substantially reducing inference cost.