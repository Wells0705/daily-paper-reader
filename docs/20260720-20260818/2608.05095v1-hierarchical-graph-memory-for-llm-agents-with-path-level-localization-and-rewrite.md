---
title: Hierarchical Graph Memory for LLM Agents with Path-level Localization and Rewrite
title_zh: 用于LLM智能体的分层图记忆：路径级定位与重写
authors: "Xiawei Yue, Boran Wang, Xiaoqing Zhang, Shuxin Zheng, Ziwei Zhang"
date: 2026-08-05
pdf: "https://arxiv.org/pdf/2608.05095v1"
tags: ["query:context-mgmt"]
score: 9.0
evidence: LLM智能体分层图记忆架构
tldr: 现有图记忆方法将记忆存储于扁平图中，导致检索时引入无关上下文且更新效率低。HiGram提出层级图记忆结构，将记忆组织为粗到细的层次，并通过基于MicroGraph的路径级定位与协同改写，仅更新局部证据路径。在长程对话问答与冲突感知记忆评估上，HiGram在答案质量和令牌效率上均优于基线，并在动态、静态和条件冲突下提升证据选择准确性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.05095v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1805, \"height\": 916, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.05095v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1843, \"height\": 538, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.05095v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 798, \"height\": 1009, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.05095v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1845, \"height\": 656, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.05095v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 879, \"height\": 349, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.05095v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 856, \"height\": 221, \"label\": \"Table\"}]"
motivation: 现有扁平图记忆检索易引入无关信息，且单元独立更新导致重复改写，效率低。
method: HiGram构建层级图记忆，用MicroGraph定位证据路径，并协同改写单元内与单元间依赖。
result: 在长程对话问答和冲突感知评测上，答案质量与令牌效率均显著提升。
conclusion: 层级结构加路径级重写有效提升长期推理记忆的准确性与更新效率。
---

## 摘要
长期推理的智能体需要一种能够随着新事实和外部反馈不断到来而高效且有效地随时间更新的记忆。近期，图记忆被采用以提供用于多跳检索和推理的结构化组织。然而，现有方法将所有记忆存储在一个平面图中，累积的历史记忆可能引入不相关的上下文，并增加检索过程中证据选择的成本。此外，它们通常独立地更新记忆单元，需要重复的单元级重写以覆盖相关变化。为解决这些问题，我们提出了HiGram，一种具有路径级定位和重写的演化分层图记忆框架。具体而言，我们首先提出了一种分层图记忆，它将记忆组织成由上层的节点和记忆单元组成的由粗到细的架构，从而减少检索过程中不相关信息量。我们进一步提出了基于微图的路径级定位，利用查询和更新条件下的微图在重写前识别支持子图和证据路径。最后，我们提出了一种协调重写方法，联合修订单元内记忆和单元间依赖关系，使有效的依赖结构能够在局部证据路径中更新。在长期对话问答和冲突感知记忆评估的基准上的实验表明，我们的方法在答案质量和词元效率方面相比基线有显著改进。此外，我们的方法在动态、静态和条件冲突下提高了答案准确性和查询有效证据选择。

## Abstract
Agents for long term reasoning require a memory that can be efficiently and effectively updated over time, as new facts and external feedback continue to arrive. Recently, graph memory has been adopted to offer structural organization for multi-hop retrieval and reasoning. However, existing methods store all memories in a flat graph, and accumulated historical memories can introduce irrelevant contexts and increase the cost of evidence selection during retrieval. Moreover, they typically update memory units independently, requiring repeated unit-wise rewrite to cover related changes. To address these issues, we propose HiGram, an evolving hierarchical graph memory framework with path-level localization and rewriting. Specifically, we first propose a hierarchical graph memory, which organizes the memory into coarse-to-fine architecture composed of upper-level nodes and MemoryUnits, thereby reducing the amount of irrelevant information during retrieval. We further propose MicroGraph-based path-level localization, which leverages query and update conditioned MicroGraphs to identify support subgraph and evidence path before rewrite. Finally, we propose a coordinated rewriting method that jointly revises intra-unit memory and inter-unit dependencies, enable valid dependency structures updating in the localized evidence path. Experiments on benchmarks for long-term conversational question answering and conflict-aware memory evaluation demonstrate that our method demonstrate substantial improvements over baselines in answer quality and token efficiency. Besides, our method improves answer accuracy and query-valid evidence selection under dynamic, static, and conditional conflicts.

---

## 论文详细总结（自动生成）

# 用于LLM智能体的分层图记忆：路径级定位与重写（HiGram）——论文总结

## 1. 核心问题与整体含义

- **研究背景**：长期推理的LLM智能体需要记忆机制，能够随着新事实、修正和外部反馈不断到达而高效更新。现有的图记忆方法（如A-MEM、Mem0等）虽然提供了结构化组织，但存在两个关键问题：
  - **扁平图结构导致检索污染**：所有历史记忆存储在一个单一平面图中，随着记忆累积，检索时容易引入大量无关上下文，增加证据筛选成本。
  - **单元独立更新导致重写冗余**：现有方法通常独立更新单个记忆单元，而答案往往由相互连接的证据路径支撑，独立更新容易遗漏相关证据，并使得过期依赖残留，需要反复进行单元级重写，造成高昂的token开销和低更新效率。
- **核心观点**：记忆的组织粒度和更新粒度应当与推理时所用的证据结构相匹配——先定位查询相关子图，再识别受更新影响的证据路径，并在受限区域内联合修改记忆状态及其依赖关系。

## 2. 方法论

### 2.1 核心思想
HiGram（Hierarchical Graph Memory）提出一种“由粗到细”的层级图记忆架构，配合**路径级定位**与**协同重写**，实现高效、一致的记忆演化。

### 2.2 关键技术细节

- **分层记忆组织（Hierarchical Memory Organization）**
  - 记忆被组织为层级图 \( G_t = (V_t, E_t) \)。
  - 节点分为两类：上层节点（主语节点、对象类别节点、上下文节点）和底层**MemoryUnit**（记忆单元）。
  - MemoryUnit存储细粒度事实（如主语、关系、对象、对象类别、时间、上下文、置信度、状态等），并通过**依赖边**（\( E_{dep} \)）互相连接，形成显式证据依赖。
  - 上层节点为MemoryUnit提供抽象索引，使得检索可以**先定位粗粒度区域，再访问细粒度记忆**，避免遍历整个历史图。

- **MicroGraph路径级定位（Path-level Localization）**
  - 定义**MicroGraph**：由一对“主语节点+对象类别节点”界定的局部子图，不增加额外记忆内容，只作为检索和定位的局部区域。
  - 给定查询 \( q_t \) 和更新 \( u_t \)，先构造**临时MemoryUnit**，提取锚点（主语、对象类别）。
  - 根据锚点匹配候选MicroGraph，排序后取Top-\( K_g \) 个，构成**支持子图（Support Subgraph）**，缩小搜索空间。
  - 在支持子图内枚举至多 \( K_p \) 条候选证据路径，根据与临时MemoryUnit的一致性（属性匹配、依赖一致性、时间有效性、上下文兼容性）打分，选出**受影响证据路径** \( \hat{P}_t \)，作为重写区域。

- **协同重写（Coordinated Rewriting）**
  - **单元内重写（Intra-unit Rewrite）**：将临时MemoryUnit与证据路径中的现有MemoryUnit匹配，更新其内部状态（如状态变为active、时间信息等），新事实则作为新active MemoryUnit提交。
  - **单元间重写（Inter-unit Rewrite）**：检查受影响MemoryUnit的直接依赖者，若依赖证据在更新后失效，则将对应MemoryUnit标记为过时并从当前证据视图排除；不自动继承旧依赖，只保留有有效证据支撑的依赖。
  - 两种重写在**同一个局部证据路径**内协同执行，区域之外保持不变。

## 3. 实验设计

### 3.1 数据集与Benchmark

- **LoCoMo**（长期对话问答）：问题分为五类：Single Hop、Multi-Hop、Open Domain、Temporal、Adversarial。指标：token级F1、BLEU-1、LLM-Judge分数（LLM-J），以及最终回答生成的Token Length。
- **MemConflict**（冲突感知记忆评估）：包含三类冲突：Dynamic（时序有效性）、Static（抵抗错误替换）、Conditional（条件适用性）。指标：Answer Accuracy (AA)、Update Order Consistency Score (UOCS)、Conflict Recognition Score (CRS)、Macro-AA、SEH@3（黄金证据进入TOP3比例）、Support Rank Score (SRS)。

### 3.2 对比方法

- LoCoMo基准：LoCoMo（全上下文）、MemoryBank、A-MEM、ReadAgent、MemGPT、Mem0。
- MemConflict基准：除A-MEM、Mem0外，还对比LangMem、Letta、MemOS。

### 3.3 实现细节

- 生成模型：LoCoMo使用GPT-5.4和GPT-4o；MemConflict使用GPT-5.0 mini。
- 评测模型：GPT-5.4 mini作为judge。
- 超参数：路径深度 \( h=3 \)，\( K_g=12 \)，证据路径预算 \( K_p=24 \)。

## 4. 资源与算力

- **论文未明确说明**所使用的GPU型号、数量、训练时长或推理部署算力。
- 仅提及了具体使用的LLM API模型（GPT-5.4、GPT-4o、GPT-5.0 mini等），但未报告这些模型的调用成本、硬件环境或训练细节。因此无法评估算力需求。

## 5. 实验数量与充分性

- **实验组数**：主要包含三大块：
  - LoCoMo主实验（两种LLM、五类问题，共10组性能对比）；
  - MemConflict主实验（三类冲突+整体指标）；
  - 消融实验（MicroGraph移除、支持子图移除，覆盖三类问题）；
  - 更新策略对比（Append-only、Relation-level、HiGram）；
  - 超参数敏感性分析（\( K_g \) 和 \( K_p \) 的取值变化）。
- **充分性评价**：
  - **优点**：数据集覆盖了长期问答和冲突感知两类核心场景，基线选择较全面（包括全上下文、文本记忆、图记忆、分层记忆等）；消融实验验证了各模块贡献；更新策略对比说明了协同重写的优势；超参数敏感性分析增强了方法鲁棒性的可信度。
  - **不足**：消融实验仅报告了选中类别的F1/LLM-J，未展示BLEU和所有类别；MemConflict上未提供HiGram关于证据选择指标的逐类消融；没有在不同LLM骨干下（除GPT-5.4/4o外）验证MemConflict结果；超参数敏感性分析仅在LoCoMo上，且只变化单参数，未考虑联合影响。整体而言实验设计较充分，但仍有提升空间。

## 6. 主要结论与发现

- HiGram在**LoCoMo**上：在GPT-5.4和GPT-4o两种模型下均取得平均F1、BLEU、LLM-J的最高成绩；尤其**Temporal类问题提升显著**，Adversarial类也表现优秀，但Open Domain类相对较弱。
- HiGram在**token效率**上优势明显：在GPT-5.4下平均token消耗约2032，仅为全上下文方法的约7.2%；在GPT-4o下约为ReadAgent的15.8%，同时保持更高的答案质量。
- 在**MemConflict**上：HiGram整体性能最强，Overall AA、Macro-AA、SEH@3、SRS均列第一；在Dynamic中UOCS最高，在Static中AA和CRS大幅领先，在Conditional中AA也最佳，表明其能有效处理动态、静态和条件冲突。
- 消融实验表明：**MicroGraph组织**和**支持子图**均对检索和推理质量有增益；移除它们会显著降低LLM-J，并增加token消耗。
- 更新策略对比显示：HiGram避免了“提升静态准确性却牺牲动态适应性”的权衡，在两种冲突上均优于Append-only和Relation-level更新。
- 超参数敏感性较低，方法对 \( K_g \)、\( K_p \) 的选择不敏感，稳健性好。

## 7. 优点

- **层级粗到细设计**：用上层抽象节点组织记忆单元，有效隔离无关历史，降低检索噪声。
- **路径级定位**：先定位MicroGraph，再选证据路径，缩小重写范围，并同时考虑查询和更新的影响，避免了全局重复搜索。
- **协同重写**：将单元内状态更新和单元间依赖重写统一在同一局部区域，保持证据链一致性，防止过期依赖残留。
- **token效率突出**：在保持高质量答案的同时大幅减少生成调用所需的上下文长度，对实际部署友好。
- **冲突处理能力强**：在动态、静态、条件三类冲突场景下均优于基线，表现出良好的记忆一致性维护能力。
- **实验多种多样性**：覆盖多类问题、多种基线、消融和超参分析，验证较全面。

## 8. 不足与局限

- **未报告算力/资源开销**：缺少GPU训练/推理成本，无法评估方法在资源受限场景的可行性。
- **Open Domain表现一般**：当所需外部知识不在历史记忆中时，方法依赖记忆检索的局限性显现，说明其不具备外部知识整合能力。
- **依赖LLM结构化提取**：MemoryUnit的构造、锚点提取、路径打分都依赖LLM，可能受LLM幻觉或提取错误影响；未对比不同提取代价。
- **路径枚举复杂度**：虽然通过Kg/Kp限制，但在超大规模记忆下，路径枚举仍可能成为瓶颈；论文未分析时间复杂度。
- **实验覆盖不完全**：消融指标不够完整；MemConflict只用GPT-5.0 mini，未跨模型验证；未进行多轮更新压力测试。
- **未来工作提及**：论文作者也指出未来需要研究外部知识整合和多模态记忆，说明当前方法集中在文本和内部记忆上。

（完）
